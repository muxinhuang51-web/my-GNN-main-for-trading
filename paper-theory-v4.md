# v4 理论推导：从三楔模型到三个原理化组件

> 2026-07-19 凌晨。目标：把 v3 的三个经验补丁（可交易标签 / 硬带宽 / 等权共识）
> 升格为从形式化推导出的组件，并给出每个组件的可证伪预测，交由今夜实验检验。

## 0. 记号与两类标签对象（关键区分）

对股票 $i$、日 $s$：潜在（完全价格发现）收益 $r^*_{is}$；观测收益被涨跌停裁剪
$$r_{is} = \mathrm{clip}(r^*_{is},\ L^-_{is},\ L^+_{is}),$$
阈值 $L^{\pm}_{is}$ **逐股逐日已知**（主板 ±10%、ST ±5%、创业/科创 ±20%）。
买入可行性 $a_{is} \in \{0,1\}$（$s{-}1$ 收盘未锁涨停且未停牌）。
触板指示 $c_{is} = \mathbb{1}[r_{is} = L^+_{is}] - \mathbb{1}[r_{is} = L^-_{is}]$（收盘封板 = 观测被删失）。

簇 $k$ 的标签有**两个不同的正当对象**，混淆它们是 v1 一切幻觉的根源：

- **对象 A（可执行标签）**：$y^E_{ks} = \mathrm{mean}\{r_{is}: i \in k,\ a_{is}=1\}$——我们能建仓的头寸的收益。v3 的掩码**就是**它的定义，不是它的有偏估计。
- **对象 B（潜在信号）**：即使在 $a_{is}=1$ 的子集内，当日**冲板收盘**的成员（$c_{is}\ne 0$）的 $r_{is}$ 仍是 $r^*_{is}$ 的删失观测——买得进，但它当日的观测收益被截断在 $L^+$。
  在删失观测上做回归会**衰减信号**。这是掩码修不了、需要 Tobit 修的残余删失。

> v3 修复了 A 层（把标签定义搬到可达过程上）；v4-C1 修复 A 层内部的 B 层残余。

## 1. P1：名义标签训练为何必然失败（v3 已实证，此处给机制模型）

两类型模型：簇 $k$ 当日成员分为锁定型（占比 $\pi_k$，延续收益 $\mu_L \gg 0$，不可买）
与自由型（$1-\pi_k$，均值 $\mu_F$，与近期强势负相关：$\mu_F = -\beta \pi_k + \varepsilon$，$\beta>0$ 为反转强度）。

名义簇标签 $y_k = \pi_k \mu_L + (1-\pi_k)\mu_F$。对 $\pi_k$ 求导：
$$\partial y_k / \partial \pi_k = \mu_L - \mu_F - (1-\pi_k)\beta > 0 \quad (\mu_L \text{ 足够大时})$$
故任何拟合 $y_k$ 的模型学到的分数 $\hat{s}_k$ 单调于 $\pi_k$（锁定占比探测器）。
但可执行收益 $y^E_k = \mu_F = -\beta\pi_k + \varepsilon$ 单调**递减**于 $\pi_k$。于是：

- $\mathrm{IC}(\hat{s}, y) > 0$（对名义标签预测正确）
- $\mathrm{IC}(\hat{s}, y^E) < 0$ 在高分区、$\approx 0$ 全截面（实测：+0.095 vs −0.0015）
- top-k 选择价值 $= \mathbb{E}[y^E \mid \hat{s} \text{ 最大}] < 0$（实测：两端各 −80bp/日）

**推论（执行端过滤的不可救药性）**：决策时剔除不可买成员不改变 $\hat{s}$ 对 $\pi$ 的单调性，
高分处的可买残差依旧是高 $\pi$ 簇的反转型成员。污染在表征，不在行动集。∎（v2 实测印证）

## 2. P2：删除 vs 修正（C1 组件：Tobit-EM）

对象 B 的估计问题：在可买子集内拟合 $\mathbb{E}[r^*|x]$，但观测为 $r=\mathrm{clip}(r^*)$。

**删除式**（Du 2025；也即把冲板样本丢出标签）产生截断偏差：
$$\mathbb{E}[r \mid r < L^+] = \mu - \sigma\frac{\phi(\alpha)}{\Phi(\alpha)} < \mu, \quad \alpha = \tfrac{L^+-\mu}{\sigma}$$
且偏差恰在 $\mu$ 大（信号强）处最大——**删除是 MNAR 下的系统性做空自己的信号**。

**修正式（Tobit-EM，阈值已知）**：设 $r^*|x \sim N(f(x), \sigma^2)$。
E 步用截断正态条件期望插补右删失样本：
$$\hat{r}^*_i = f(x_i) + \sigma\,\frac{\phi(\alpha_i)}{1-\Phi(\alpha_i)}, \quad \alpha_i = \tfrac{L^+_i - f(x_i)}{\sigma}$$
（左删失镜像）；M 步在插补标签上重拟合 $f$（我们的 Ridge 有闭式解，收敛快）；
$\sigma$ 由未删失残差估计。标准 EM 单调性保证似然不降；正态假设下一致。

**可证伪预测 T1**：当标签窗口内冲板占比非平凡时，Tobit-EM 臂 > 掩码臂 > 删除臂
（按可执行毛超额与配对差衡量）；冲板占比≈0 时三臂应无差异。

## 3. P3：极值收缩（C3 组件：删失感知 Tweedie）

分数模型 $z_i = \theta_i + \epsilon_i$，$\epsilon_i \sim N(0, \sigma_i^2)$。Tweedie/Robbins 公式：
$$\mathbb{E}[\theta_i \mid z_i] = z_i + \sigma_i^2 \cdot \frac{d}{dz}\log m(z_i)$$
其中 $m$ 为 $z$ 的截面边际密度（当日用 Lindsey 泊松样条估计）。
推导：$m(z)=\int \varphi_{\sigma}(z-\theta)\,dG(\theta)$，对 $z$ 求导并除以 $m$ 得
$m'(z)/m(z) = (\mathbb{E}[\theta|z]-z)/\sigma^2$。∎

性质：(i) 密度尾部 $\log m$ 斜率为负 → 极端分自动向内收缩（**赢家诅咒的解析解**）；
(ii) $\sigma_i^2$ 越大收缩越狠 → 用 9 模型分歧方差 $s_i^2$ 做插值即得逐股异质精度加权；
(iii) v3.3 的硬带宽 = 该后验加权组合的 0-1 近似（把收缩后排序的中上段取 1、两端取 0）。

**删失感知扩展**：锁定名的 $z_i$ 建立在删失信息上，其有效噪声更大——
以截断正态方差修正系数 $1 + \kappa(\alpha_i)$ 膨胀其 $\sigma_i^2$，使"删失处收缩更狠"内生化。

**可证伪预测 T2**：Tweedie-top-k ≥ 硬带宽 ≈ Grinold(IC缩放+3σ截尾) > 原始分 top-k；
删失感知版 ≥ 普通版，且优势集中在锁定占比高的日子。

## 4. P4：可行性影子价格（C2 组件）

真实目标 $\max_{w\ge 0,\ \sum w \le 1} \mathbb{E}[\sum_i w_i r_i \mid a_i=1]$，
将机会约束（建仓失败率 ≤ 预算）拉格朗日化，得线性化选择规则：
$$\tilde{s}_i = \hat{\theta}_i - \lambda\, \hat{P}(\text{锁板}_{t+1}\mid x_i), \qquad \lambda \ge 0$$
$\hat{P}$ 是**监督**学习问题（指示器逐样本可观测）。悲观版用上置信界替代 $\hat{P}$。

**可证伪预测 T3**：存在 $\lambda^* > 0$ 使 $\tilde{s}$-top-k 优于 $\hat{\theta}$-top-k；
$\lambda$ 过大退化为纯防御组合，呈内点最优。

## 5. 今夜实验对三个预测的判决设计

- E1（判 T1）：标签三臂 {Tobit-EM, 掩码, 删除} × 9 单模型（3es×3km）配对 + 各自共识，band 与 top-k 双收割口径
- E2（判 T2）：共识分后处理四臂 {硬带 60-90, Grinold 基线, Tweedie, 删失感知 Tweedie}，同一批分数
- E3（判 T3）：$\tilde{s} = $ Tweedie 分 $- \lambda \hat{P}$，$\lambda \in \{0, 0.5, 1, 2, 4\}\cdot\hat{\sigma}_z$，top-100 收割
- 全部 valid 2022-2023；test 冻结不动；配对 NW t + 全套摩擦成本

---

## 6. 晨后修订（P3′）：实验驳回 P3 后的正确尾部模型

实测（2026-07-19 通宵矩阵，见 report-overnight-v4.md）：Tweedie/EB 收缩在真实数据上
显著劣于硬带宽（−1.9 vs +4.3），且尾部跨模型分歧仅为中段的 0.62 倍。

修订：尾部分数 $z = \theta + b + \epsilon$，$b$ 为删失签名经表征投影的**共享偏差**
（各模型在同一删失过程上训练，$b$ 跨模型相关）。集成平均消 $\epsilon$ 不消 $b$；
EB 以 $s^2$（仅度量 $\epsilon$）定收缩强度，对 $b$ 无效，精度加权反而偏好低 $\epsilon$ 高 $b$ 样本。
当尾部 $|b|$ 大且符号不可识别时，对污染区整体排除（带收割）是极小极大最优决策——
v3.3 带宽由此获得理论地位。P1-P4 中仅 P1（失败机制）与 P2 的方向性结论在体内成立；
P3 的纯噪声假设被证伪并由 P3′ 取代；P4 在尾部偏差主导下失去作用空间。
