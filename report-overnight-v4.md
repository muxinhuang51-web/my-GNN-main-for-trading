# 通宵战役晨报：理论推导 × 实验判决（v4）

> 2026-07-19 晨。设计见 paper-theory-v4.md；38 组实验全部完成于 valid 期（test 仍冻结）。
> 一句话总结：**三个"原理化升级"全部被真实数据驳倒，而驳倒方式揭示了一个更深的结构性发现——
> 尾部是共识偏差而非估计噪声——这给 v3.3 的"粗糙"配方提供了它此前缺失的理论正当性。**

## 1. 判决总表

| 预测 | 内容 | 判决 | 证据 |
|---|---|---|---|
| T1 | Tobit-EM 修正 > 掩码 > 删除 | **驳回**（修正≈掩码） | 配对 tobit−mask：+0.01，t=0.0；tobit−delete：+0.34，t=1.8（弱） |
| T2 | Tweedie 收缩 ≥ 硬带宽 > 原始 top-k | **强烈驳回**（方向反转） | band +4.28 / grinold +0.15 / tweedie **−1.42** / ct_tweedie **−1.87**（t=−2.8） |
| T3 | 存在 λ*>0 改善选择 | **驳回**（λ 惰性） | λ∈{0.5..4} 全部 ≈ −1.86，与基线无差异 |

体外（合成数据）三测全过、体内全败——**机制成立但其前提假设在真实数据中不满足**，差异本身即是发现。

## 2. E1：标签三臂（9 配对单模型，band 收割）

- tobit ≈ mask（差 +0.01）：掩码内部的冲板删失在簇均值层面是二阶效应——**掩码已捕获标签楔子的全部一阶量**，理论中"对象 B 残余删失"实际可忽略。对论文是好消息：最简修复即充分
- 两者均弱优于 delete（+0.34，t≈1.3-1.8，不显著）：删除偏差方向如预测但量级小
- 单模型水平仍由编码器种子主导（es42≈5.4 / es7≈2-4 / es123≈0.4）——共识的必要性再次确认

## 3. E2：后处理四臂（同一批共识分）——今夜最重要的结果

| 臂 | 毛超额 (NW t) | 净 |
|---|---|---|
| 硬带宽 60-90（v3.3 复刻 ✓） | **+4.28 (+6.26)** | **+3.09** |
| Grinold 截尾 + top100 | +0.15 (+0.22) | −0.97 |
| Tweedie + top100 | −1.42 (−2.16) | −2.56 |
| 删失感知 Tweedie + top100 | −1.87 (−2.79) | −2.99 |

三个机制层解释（均已独立验证）：
1. Grinold 截尾是单调变换 → 选择与原始 top-k 完全相同（+0.15 与 v3.2 共识 top-k 精确一致）
2. **Tweedie 更糟的原因：精度加权主动选中伪信号**——EB 按分歧方差收缩，而实测**尾部分歧只有中段的 0.62 倍**（8 抽样日中位数）：九个模型在删失签名上高度一致 → 低分歧 → 收缩最少 → 排名更靠前
3. λ 惰性同理：尾部偏差远大于 P(锁板) 惩罚的重排能力

## 4. 理论修订（P3′，替换 paper-theory-v4.md 的 P3 假设）

尾部分数的正确模型不是 z = θ + ε（赢家诅咒/纯噪声），而是
**z_tail = θ + b + ε**，其中 b 为删失签名经表征投影产生的**共享偏差**——
九个模型在同一删失过程上训练，b 跨模型相关：集成平均消 ε 不消 b；
EB 收缩以 s²（只度量 ε）定强度，对 b 无能为力，且精度加权反而偏好低 ε 高 b 的名字。
当尾部 |b| 大且符号未知时，**对偏差污染区的整体排除（带收割）是极小极大最优响应**——
v3.3 的硬带宽由此从"粗糙启发式"升格为"偏差结构下的正确决策规则"。

## 5. 对论文的落位（意外的强化）

1. **方法章节大幅简化且更硬**：获胜配方 = 可执行标签（掩码）+ 带收割 + 等权共识——
   三个组件各自击败了自己的教科书式"原理化"挑战者（Tobit-EM / EB-Tweedie / 影子价格），
   审稿人所有"为什么不用 X"的问题都已被数据预先回答
2. **新增一个独立贡献**：删失训练产生的**跨模型共享偏差**——集成与 EB 的失效条件及其度量
   （尾部/中段分歧比值 0.62）；"偏差不是噪声"是对赢家诅咒叙事的实证否定
3. E4 全组合确认：tobit+band ≈ band（3.99 vs 4.28，种子噪声内）——最终配置锁定 v3.3 形态

## 6. 当前最优配置（供论文级矩阵与 test 冻结运行使用）

掩码标签（可交易成员均值）→ 9 模型（3 编码器 × 3 KMeans 种子）逐股共识 →
60-90 分位带持有 → h=5 调仓：**毛 +4.28（t=6.3）/ 净 +3.09 / 换手 0.09**（valid 期）。
带宽超参此前已在 valid 上选定，test 期不得再调。
